name: GPagesDeploy

on:
  push:
    branches: [ dev ]
    
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false
          
      - name: Install and Build 🔧
        run: |
          echo "module.exports = {basePath: '/LAATA_pk_page', assetPrefix: '/LAATA_pk_page/',}" > next.config.js
          npm ci
          npm run static-build
      
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.7
        with:
          branch: gh-pages
          folder: out
